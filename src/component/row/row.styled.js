import { isStyledComponent } from "styled-components";
import styled from 'styled-components' //am importat styled dintr-o librarie deja importata din React

export const StyledRow = styled.div`
  grid-column: 1/-1;
`
